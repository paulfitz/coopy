#include <coopy/ColumnInfo.h>

#include <stdio.h>

using namespace coopy::store;

std::string ColumnInfo::getName() const {
  //char buf[256];
  //sprintf(buf,"%d",index);
  //return buf;
  return "";
}

