if (HAVE_TR1)
  ADD_DEFINITIONS("-std=c++0x" -DHAVE_TR1)
endif ()

INCLUDE_DIRECTORIES(${SHEET_INCLUDE_DIRS})
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

IF (COMPILE_PLAIN_EXECUTABLES)
  ADD_EXECUTABLE(csvpatch sspatch.cpp)
  TARGET_LINK_LIBRARIES(csvpatch sheet)
  INSTALL(TARGETS csvpatch COMPONENT ${CSVBIN} DESTINATION bin)
ENDIF ()

ADD_EXECUTABLE(sspatch sspatch.cpp)
TARGET_LINK_LIBRARIES(sspatch coopy_full)

INSTALL(TARGETS sspatch COMPONENT ${BIGBIN} DESTINATION bin)

