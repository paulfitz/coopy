INCLUDE_DIRECTORIES(${SHEET_INCLUDE_DIRS})
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

ADD_EXECUTABLE(ssformat ssformat.cpp)
TARGET_LINK_LIBRARIES(ssformat sheet)

INSTALL(TARGETS ssformat COMPONENT utilities DESTINATION bin)
