INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
ADD_LIBRARY(coopy_csv csv.h libcsv.c)
SET_TARGET_PROPERTIES(coopy_csv PROPERTIES LINKER_LANGUAGE CXX)
export(TARGETS coopy_csv APPEND FILE ${COOPY_DEPENDENCIES})
