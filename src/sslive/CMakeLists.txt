INCLUDE_DIRECTORIES(${SHEET_INCLUDE_DIRS})
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

IF (USE_YARP)

  FIND_PACKAGE(YARP REQUIRED)
  INCLUDE_DIRECTORIES(${YARP_INCLUDE_DIRS})
  LINK_LIBRARIES(${YARP_LIBRARIES})

  IF (COMPILE_PLAIN_EXECUTABLES)
    ADD_EXECUTABLE(csvlive sslive.cpp)
    TARGET_LINK_LIBRARIES(csvlive sheet)
    INSTALL(TARGETS csvlive COMPONENT ${CSVBIN} DESTINATION bin)
  ENDIF ()
  
  ADD_EXECUTABLE(sslive sslive.cpp)
  TARGET_LINK_LIBRARIES(sslive coopy_full)
  
  INSTALL(TARGETS sslive COMPONENT ${BIGBIN} DESTINATION bin)

ENDIF()
